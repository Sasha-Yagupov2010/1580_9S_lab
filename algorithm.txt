 
'''
функция вывода часов
    вывод часов в формате целое число
    находим последнюю цифру числа
    
    если 11 <= часы <= 14: выводим "часов"
    иначе если последняя цифра числа = 1: выводим "час"
    иначе если 2 <= последняя цифра числа <= 4: выводим "часа"
    иначе: выводим "часов"




функция перевод минут
    если минуты не равны 0, то
    
        вывод минут в формате целое число
        находим последнюю цифру числа
        
        если  11 <= минуты <= 14: выводим "минут"
        иначе если последняя цифра числа = 1: выводим "минута"
        иначе если 2 <= последняя цифра числа <= 4: выводим "минуты"
        иначе: выводим "минут"



вывод времени дня
    если 0 <= часы < 6: выводим "ночи"
    иначе если 6 <= часы < 12: выводим "утра"
    иначе если 12 <= часы < 18: выводим "дня"
    иначе: выводим "вечера"


вывод ровно
    если минуты = 0: выводим "ровно"


проверка введенных часов
   если часы >= 0 и часы <= 23: все хорошо
   иначе: пользователь что то не так ввел


проверка введенных минут
    если минуты >= 0 и минуты <= 59: все хорошо
    иначе: пользователь что то не так ввел


основная часть программы:
    просим ввести время в нужном формате
    получаем данные пользователя
    проверяем сколько чисел введено
    
    пробуем преобразовать введенные строки в числа
    если не получается - ошибка
    
    проверяем часы на корректность значений(от 0 до 23) 
    проверяем минуты на корректность значений(от 0 до 59)


    если часы = 0 и минуты = 0:  "полночь"
    если часы = 12 и минуты = 0: "полдень"
    иначе:
        пробуем вывести числа( функция вывода часов и функция перевод минут )
        выводим ровное ли время(минуты = 0)
        выводим какое время суток




запускаем программу именно с основной части
    
    '''
